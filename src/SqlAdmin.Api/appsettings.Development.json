{
  // Overrides applied only when ASPNETCORE_ENVIRONMENT=Development (set by launchSettings.json / dotnet watch).
  // More verbose logging while developing; production keeps the quieter defaults from appsettings.json.
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  }
}
